# FinalPriceAuctionWebsite
Install Reac package:
```bash
sudo apt install npm nodejs
```
Then install the dependencies:
```bash
npm update
```
To run the project, use:
```bash
npm run host -- --host 0.0.0.0
``` 